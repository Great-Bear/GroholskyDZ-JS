
@{
    ViewData["Title"] = "ListProduct";
}
@Html.Partial(@"Partial\Header")
<ul id="DataList">
    @for (int i = 0; i < Model.Count; i++)
    {
        <li>@Model[i].ID. @Model[i].Name-@Model[i].Price$ (@Model[i].CreatedDate)</li>
    }
</ul>
@Html.Partial(@"Partial\Footer")

